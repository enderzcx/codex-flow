{
  "name": "codex-flow",
  "version": "0.4.0",
  "description": "Codex-native dynamic workflow runner MVP.",
  "type": "module",
  "bin": {
    "cwf": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run tests",
    "check": "npm run build && npm test",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.136.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=18"
  }
}
